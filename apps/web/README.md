# Landfill web client

The React browser interface for Landfill. Run it from the repository root with
`npm run dev`; development API requests use the same-origin `/api` path and are
proxied according to `API_PROXY_TARGET` in `.env`.

See the root README for setup, architecture, security, and contribution notes.
