# Docker Compose publishes Landfill only on this machine by default.
# Change to 0.0.0.0 to make it reachable from a trusted local network.
LANDFILL_BIND_ADDRESS=127.0.0.1

# Port used to open Landfill in the browser.
LANDFILL_PORT=8080
